# --- Data (large external dataset — obtain from Kaggle, see README) ---
/data/

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- Virtual environments ---
.venv/
venv/
env/
.env

# --- Reproducible build outputs (regenerated by the pipeline in ~15 s) ---
# Kept out of git: large/binary or derived. The small, human-readable decision
# records (selection.json, thresholds.json) and the review HTML are tracked.
artifacts/signal_table.parquet
artifacts/chartdata.json
artifacts/*.log

# --- Editor / OS ---
.vscode/
.idea/
.DS_Store
*.swp
